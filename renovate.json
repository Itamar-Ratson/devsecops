{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "labels": ["dependencies"],
  "packageRules": [
    {
      "description": "Pin GitHub Actions to SHA digests and auto-merge digest updates",
      "matchManagers": ["github-actions"],
      "pinDigests": true,
      "matchUpdateTypes": ["digest"],
      "automerge": true
    },
    {
      "description": "GitHub Actions minor/patch updates require manual review",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": false
    },
    {
      "description": "Group Helm chart dependency updates",
      "matchManagers": ["helmv3"],
      "groupName": "helm-dependencies"
    },
    {
      "description": "Group Terraform provider updates",
      "matchManagers": ["terraform"],
      "groupName": "terraform-providers"
    },
    {
      "description": "Group Dockerfile base image updates",
      "matchManagers": ["dockerfile"],
      "groupName": "docker-base-images"
    },
    {
      "description": "Group inline tool version updates from workflow run blocks",
      "matchManagers": ["custom.regex"],
      "groupName": "workflow-tool-versions"
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Tool versions annotated with '# renovate:' comments in workflow files",
      "fileMatch": ["^\\.github/workflows/.*\\.yaml$"],
      "matchStrings": [
        "#\\s*renovate:\\s+datasource=(?<datasource>[^\\s]+)\\s+depName=(?<depName>[^\\s]+)(?:\\s+extractVersion=(?<extractVersion>[^\\s]+))?\\n[^\\n]*?(?<currentValue>\\d+\\.\\d+\\.\\d+)"
      ]
    }
  ],
  "schedule": ["after 9am and before 5pm every weekday"]
}
