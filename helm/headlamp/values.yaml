# Gateway configuration
gateway:
  name: main-gateway
  namespace: gateway

# Headlamp configuration
headlamp:
  replicaCount: 1

  # Downgrade to v0.28.0 - last version with working OIDC refresh tokens
  # See: https://github.com/kubernetes-sigs/headlamp/issues/3143
  image:
    registry: ghcr.io
    repository: headlamp-k8s/headlamp
    tag: "v0.28.0"

  config:
    oidc:
      # Do not create secret from values - we use external secret from VSO
      secret:
        create: false
      # Client secret from VaultStaticSecret
      externalSecret:
        enabled: true
        name: headlamp-oidc-secret
    # Note: -oidc-ca-file not available in v0.28.0
    # Using SSL_CERT_FILE env var instead for CA verification
    extraArgs: []

  # Cluster discovery from ArgoCD secrets
  # Headlamp reads secrets with label: argocd.argoproj.io/secret-type=cluster
  clusterRoleBinding:
    create: true
    clusterRoleName: cluster-admin

  service:
    type: ClusterIP
    port: 80

  # Disable built-in ingress (we use HTTPRoute)
  ingress:
    enabled: false

  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi

  # Mount CA bundle from trust-manager for OIDC TLS verification
  volumes:
    - name: ca-bundle
      configMap:
        name: gateway-ca-bundle
        optional: true  # Don't fail if trust-manager hasn't synced yet

  volumeMounts:
    - name: ca-bundle
      mountPath: /etc/ssl/certs/gateway-ca.crt
      subPath: ca.crt
      readOnly: true

  # Point Go's TLS to use our CA bundle
  env:
    - name: SSL_CERT_FILE
      value: /etc/ssl/certs/gateway-ca.crt

  # Run as non-root
  securityContext:
    runAsNonRoot: true
    runAsUser: 100
    runAsGroup: 101

  podSecurityContext:
    fsGroup: 101
