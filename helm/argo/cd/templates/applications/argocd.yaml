# Wave 0 — Self-management: ArgoCD
# Dependencies: none
# Note: creates VaultStaticSecret (VSO CRD, wave 3) and ServiceMonitor (monitoring CRD, wave 7).
# SkipDryRunOnMissingResource prevents wave 0 from blocking the rest of the sync tree.
{{- if .Values.gitops.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd
  namespace: {{ .Release.Namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: "{{ .Values.gitops.repoURL }}"
    targetRevision: "{{ .Values.gitops.targetRevision }}"
    path: helm/argo/cd
    helm:
      valueFiles:
        - ../../ports.yaml
        - values.yaml
        - values-argocd.yaml
      parameters:
        - name: gitops.repoURL
          value: "{{ .Values.gitops.repoURL }}"
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  ignoreDifferences:
    - group: ""
      kind: Secret
      name: argocd-secret
      jsonPointers:
        - /data
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      jsonPointers:
        - /spec/parentRefs
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - RespectIgnoreDifferences=true
      - SkipDryRunOnMissingResource=true
{{- end }}
