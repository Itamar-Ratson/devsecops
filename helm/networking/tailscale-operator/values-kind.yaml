connector:
  hostname: kind-subnet-router
  routes:
    - "172.18.0.0/16"  # KinD node IPs (Docker bridge)
    - "10.244.0.0/16"  # KinD pod CIDR (native routing = real pod IPs cross-cluster)

vaultSecret:
  enabled: true
