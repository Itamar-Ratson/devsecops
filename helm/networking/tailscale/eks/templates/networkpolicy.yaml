---
# CiliumNetworkPolicy for Tailscale subnet router on EKS
# Egress: Tailscale control plane, WireGuard, DNS, K8s API, VPC CIDRs.
# Ingress: kubelet health checks, return traffic.
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: tailscale
  namespace: {{ .Release.Namespace }}
spec:
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: tailscale
  egress:
    # Allow Tailscale control plane (coordination server, DERP relay servers)
    - toEntities:
        - world
      toPorts:
        - ports:
            - port: "443"
              protocol: TCP
    # Allow WireGuard UDP (direct peer connections + DERP relay)
    - toEntities:
        - world
      toPorts:
        - ports:
            - port: "41641"
              protocol: UDP
            - port: "3478"
              protocol: UDP
    # Allow DNS resolution (CoreDNS in kube-system)
    - toEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: kube-system
            k8s:k8s-app: kube-dns
      toPorts:
        - ports:
            - port: "{{ .Values.ports.dns }}"
              protocol: UDP
    # Allow Kubernetes API access (for storing state in Secret)
    - toEntities:
        - kube-apiserver
      toPorts:
        - ports:
            - port: "443"
              protocol: TCP
    # Allow routing to advertised subnets (subnet router function)
    - toCIDR:
        {{- range .Values.routes }}
        - {{ . }}
        {{- end }}
  ingress:
    # Allow kubelet probes and host access
    - fromEntities:
        - host
        - remote-node
        - kube-apiserver
    # Allow return traffic from advertised subnets
    - fromCIDR:
        {{- range .Values.routes }}
        - {{ . }}
        {{- end }}
