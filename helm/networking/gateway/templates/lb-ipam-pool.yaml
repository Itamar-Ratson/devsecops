# cluster-scoped: Cilium LB-IPAM assigns IPs from this pool to gateway LoadBalancer services
apiVersion: cilium.io/v2alpha1
kind: CiliumLoadBalancerIPPool
metadata:
  name: kind-lb-pool
spec:
  blocks:
    - cidr: {{ .Values.lbPool.cidr | quote }}
