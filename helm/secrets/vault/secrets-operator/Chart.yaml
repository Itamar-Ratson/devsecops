apiVersion: v2
name: vault-secrets-operator
description: HashiCorp Vault Secrets Operator for syncing Vault secrets to Kubernetes
version: 1.0.0
dependencies:
  - name: vault-secrets-operator
    version: "0.10.0"
    repository: "https://helm.releases.hashicorp.com"
