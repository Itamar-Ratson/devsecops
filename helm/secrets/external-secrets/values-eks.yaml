# eksCluster.name used to compute deterministic IRSA role ARN:
# arn:aws:iam::<accountId>:role/<eksCluster.name>-eso
# accountId comes from globals.yaml, eksCluster.name from eks-resources values
eksCluster:
  name: devsecops-eks
