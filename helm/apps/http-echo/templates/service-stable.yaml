apiVersion: v1
kind: Service
metadata:
  name: {{ if .Values.rollout.enabled }}http-echo-stable{{ else }}http-echo{{ end }}
spec:
  ports:
    - port: {{ .Values.ports.httpService }}
      targetPort: {{ .Values.ports.httpEcho }}
  selector:
    app: http-echo
