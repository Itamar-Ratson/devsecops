apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-aws-ec2
spec:
  package: "{{ .Values.providers.awsEc2.package }}:{{ .Values.providers.awsEc2.version }}"
  packagePullPolicy: IfNotPresent
  revisionHistoryLimit: 1
