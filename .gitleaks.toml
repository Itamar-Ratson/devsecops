# .gitleaks.toml
title = "Gitleaks config"

[allowlist]
  description = "Allowlisted paths and patterns"
  paths = [
    '''terraform/live/secrets\.tfvars''',
    '''\.github/workflows/.*\.yaml''',
  ]
