# Secure K8s Environment Configuration
# Copy this file to .env and set your values
#
# IMPORTANT: Never commit .env to git!

# Transit Vault token for auto-unseal
# Generate: openssl rand -base64 32
VAULT_TRANSIT_TOKEN=change-me-to-secure-token

# Grafana admin credentials
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-me-to-secure-password

# ArgoCD admin password
# Generate bcrypt hash: htpasswd -nbBC 10 "" 'your-password' | tr -d ':\n'
# The value below is a bcrypt hash - generate a new one for your password
ARGOCD_ADMIN_PASSWORD_HASH=

# ArgoCD server secret key (for session signing)
# Generate: openssl rand -base64 32
ARGOCD_SERVER_SECRET_KEY=

# PagerDuty integration key (optional)
# Get from: PagerDuty > Services > Your Service > Integrations > Events API v2
PAGERDUTY_ROUTING_KEY=
